{
  "name": "@pi-swarm/ext-runtime",
  "private": true,
  "description": "Pi-Swarm extension layer 00-runtime: integration boundary, hook engine, transport parity, telemetry. Load order within the layer is this list; layers load in numeric prefix order.",
  "pi": {
    "extensions": [
      "pi-tmux-resurrection.ts",
      "cache-telemetry.ts",
      "swarm-update.ts",
      "bootstrap.ts",
      "hooks.ts",
      "swarm-runtime.ts",
      "swarm-transport-parity.ts"
    ]
  },
  "devDependencies": {
    "@types/node": "^22.20.2"
  }
}
