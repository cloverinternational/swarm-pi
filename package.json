{
  "name": "@pi-swarm/integration",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build -w @pi-swarm/contract && npm run build -w @pi-swarm/core && npm run build -w @pi-swarm/runtime-contracts && npm run build -w @pi-swarm/prompt && npm run build -w @pi-swarm/skills && npm run build -w @pi-swarm/autogenskills && npm run build -w @pi-swarm/policy && npm run build -w @pi-swarm/schedule && npm run build -w @pi-swarm/agents && npm run build -w @pi-swarm/mcp && npm run build -w @pi-swarm/taskmanage",
    "test": "vitest run",
    "test:parity": "node --test tests/parity-probe.test.mjs",
    "test:integration": "node ./tools/integration/run-postgres-integration.mjs",
    "parity:probe": "node tools/parity/probe.mjs --profile project --workspace . --output artifacts/parity/default",
    "dogfood": "npm run build && npm test && npm run test:parity"
  },
  "workspaces": [
    "packages/*/*"
  ],
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pg": "^8.23.1",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
