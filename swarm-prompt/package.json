{
  "name": "@pi-swarm/swarm-prompt",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run test",
    "sync": "node scripts/sync.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "@types/node": "^22.0.0",
    "vitest": "^3.2.0"
  }
}
