# INDEX.md — gold

> Modo: incremental | Directorios indexados: 1
> Para regenerar: `idx generate --path ./gold`

---

## Scope

### `gold/` — Provides `GoldStore` for persisting Gold insights and analysis runs to disk and `Analyzer` for executing the analysis.

---

## Árbol de Estructura

### `gold/` — Provides `GoldStore` for persisting Gold insights and analysis runs to disk and `Analyzer` for executing the analysis.
- `store.go` — Persists insights and runs to disk
- `analyzer.go` — Runs analysis logic
- `types.go` — Defines core data structures
**Entry:** `store.go:L1`

---

## Señales de Cambio Reciente

- `swarm-sdk/gold/analyzer.go`
- `swarm-sdk/gold/cross_project.go`
- `swarm-sdk/gold/gold_test.go`
- `swarm-sdk/gold/store.go`
- `swarm-sdk/gold/types.go`
