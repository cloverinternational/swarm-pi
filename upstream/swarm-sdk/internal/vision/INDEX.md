# INDEX.md — vision

> Modo: incremental | Directorios indexados: 1
> Para regenerar: `idx generate --path ./vision`

---

## Scope

### `vision/` — Unified image handling for LLM providers via the ImageData struct.

---

## Árbol de Estructura

### `vision/` — Unified image handling for LLM providers via the ImageData struct.
Standardizes image encoding, validation, and metadata management across providers.
- `image.go` — Core struct and encoding/decoding
- `validation.go` — Size and format validation
- `metadata.go` — Metadata extraction and conversion
**Entry:** `image.go:L1`

---

